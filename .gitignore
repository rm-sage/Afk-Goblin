node_modules/
dist/
# Built UI. The repo root doubles as the Bolt plugin directory, so the build
# lands here and main.lua serves it from plugin://app/index.html.
app/
pages/
.vite/
*.tsbuildinfo
coverage/

# Personal AfkWarden config used for local import testing — not published.
fixtures/personal/
*.local.json

.DS_Store
Thumbs.db
.env
.env.*
!.env.example
